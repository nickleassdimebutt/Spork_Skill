---
description: Bootstrap or extend the SPORK spike-workflow installation in a target repo.
---

Invoke the `spork` skill. Args (if any): $ARGUMENTS

Recognised Pro-mode flags (parse at SKILL.md Phase 0 Step 0.0; suppress the Mode picker if any are set):

- `--pro` — Full Stack (all three amplifiers; ~$2.10-$4.20, ~5-10 min, 42 subagents).
- `--pro-pass1` — Fire God Mode (multi-framing pass-1 + synthesiser; ~$0.70-$1.40, 14 subagents).
- `--pro-pass2` — Token Gobbler Mode (parallel pass-2 + red-team-per-option; ~$1.35-$2.70, 27 subagents).
- `--pro-discover` — Outer God Mode (always-on Phase 2 Explore swarm; ~$0.35-$0.70, 7 subagents).

Multiple flags compose. No flag = God Mode (default).
